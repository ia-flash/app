server {
    listen       80;
    server_name  localhost;
    location / {
        root   /static;
        index  index.html index.html;
        try_files $uri$args $uri$args/ $uri/ /index.html;
    }
}
